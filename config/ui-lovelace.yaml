views:
  - visible:
      # Sylvain
      - user: !secret user_id_sylvain
      # Cassie
      - user: !secret user_id_cassie
      # Jarvis
      - user: !secret user_id_jarvis
      # Léo
      - user: !secret user_id_leo
      # Admin
      - user: !secret user_id_admin
      # Steph
      - user: !secret user_id_steph
    badges: []
    panel: true
    cards:
      - elements:
          - type: icon
            icon: 'mdi:account-details'
            title: Naviguer vers la page Profil
            tap_action:
              action: navigate
              navigation_path: /profile
            style:
              color: var(--primary-color)
              top: 88%
              left: 47%
              border: 1px solid var(--primary-color)
              border-radius: 9px
              padding: 2px 10px
              transform: scale(1.8)
        image: /local/lovelace/redirect/404.png?v=0.0.4
        type: picture-elements
